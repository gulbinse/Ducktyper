package typeracer.communication.statuscodes;

/** General permission status code for client requests. */
public enum PermissionStatus {
  /** Status that represents the request was accepted. */
  ACCEPTED,
  /** Status that represents the request was denied. */
  DENIED
}
